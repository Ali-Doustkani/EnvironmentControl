<Window x:Class="EnvironmentControl.Views.VariableEditor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="VariableEditor" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBox Text="{Binding Name}"></TextBox>
        <StackPanel Grid.Row="1">
            <Button Command="{Binding Ok}">Ok</Button>
            <Button Command="{Binding Cancel}">Cancel</Button>
            <Button Visibility="{Binding DeleteVisibility}" Command="{Binding Delete}">Delete</Button>
        </StackPanel>
    </Grid>
</Window>
